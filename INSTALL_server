#!/bin/bash
#Install file for server
#autogenerated by update.sh
if [ -f etc/cups/cupsd.conf ] || [ -d etc/cups/cupsd.conf ] ; then
    read -p "copy /etc/cups/cupsd.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 640 "server/etc/cups/cupsd.conf" /etc/cups/cupsd.conf"
        install -D -d -o root -g lp -m 640 "server/etc/cups/cupsd.conf" /etc/cups/cupsd.conf
    fi
fi
if [ -f etc/cups/printers.conf ] || [ -d etc/cups/printers.conf ] ; then
    read -p "copy /etc/cups/printers.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 600 "server/etc/cups/printers.conf" /etc/cups/printers.conf"
        install -D -d -o root -g lp -m 600 "server/etc/cups/printers.conf" /etc/cups/printers.conf
    fi
fi
if [ -f etc/cups/classes.conf ] || [ -d etc/cups/classes.conf ] ; then
    read -p "copy /etc/cups/classes.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 600 "server/etc/cups/classes.conf" /etc/cups/classes.conf"
        install -D -d -o root -g lp -m 600 "server/etc/cups/classes.conf" /etc/cups/classes.conf
    fi
fi
if [ -f etc/cups/cups-files.conf ] || [ -d etc/cups/cups-files.conf ] ; then
    read -p "copy /etc/cups/cups-files.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 640 "server/etc/cups/cups-files.conf" /etc/cups/cups-files.conf"
        install -D -d -o root -g lp -m 640 "server/etc/cups/cups-files.conf" /etc/cups/cups-files.conf
    fi
fi
if [ -f etc/cups/cups-browsed.conf ] || [ -d etc/cups/cups-browsed.conf ] ; then
    read -p "copy /etc/cups/cups-browsed.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 644 "server/etc/cups/cups-browsed.conf" /etc/cups/cups-browsed.conf"
        install -D -d -o root -g lp -m 644 "server/etc/cups/cups-browsed.conf" /etc/cups/cups-browsed.conf
    fi
fi
if [ -f etc/cups/snmp.conf ] || [ -d etc/cups/snmp.conf ] ; then
    read -p "copy /etc/cups/snmp.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 640 "server/etc/cups/snmp.conf" /etc/cups/snmp.conf"
        install -D -d -o root -g lp -m 640 "server/etc/cups/snmp.conf" /etc/cups/snmp.conf
    fi
fi
if [ -f etc/cups/subscriptions.conf ] || [ -d etc/cups/subscriptions.conf ] ; then
    read -p "copy /etc/cups/subscriptions.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g lp -m 640 "server/etc/cups/subscriptions.conf" /etc/cups/subscriptions.conf"
        install -D -d -o root -g lp -m 640 "server/etc/cups/subscriptions.conf" /etc/cups/subscriptions.conf
    fi
fi
if [ -f etc/cups/ppd/HL3150CDW.ppd ] || [ -d etc/cups/ppd/HL3150CDW.ppd ] ; then
    read -p "copy /etc/cups/ppd/HL3150CDW.ppd ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/cups/ppd/HL3150CDW.ppd" /etc/cups/ppd/HL3150CDW.ppd"
        install -D -d -o root -g root -m 644 "server/etc/cups/ppd/HL3150CDW.ppd" /etc/cups/ppd/HL3150CDW.ppd
    fi
fi
if [ -f etc/cups/ppd/Kyocera_FS-1320D.ppd ] || [ -d etc/cups/ppd/Kyocera_FS-1320D.ppd ] ; then
    read -p "copy /etc/cups/ppd/Kyocera_FS-1320D.ppd ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/cups/ppd/Kyocera_FS-1320D.ppd" /etc/cups/ppd/Kyocera_FS-1320D.ppd"
        install -D -d -o root -g root -m 644 "server/etc/cups/ppd/Kyocera_FS-1320D.ppd" /etc/cups/ppd/Kyocera_FS-1320D.ppd
    fi
fi
if [ -f etc/dovecot/dovecot.conf ] || [ -d etc/dovecot/dovecot.conf ] ; then
    read -p "copy /etc/dovecot/dovecot.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/dovecot/dovecot.conf" /etc/dovecot/dovecot.conf"
        install -D -d -o root -g root -m 644 "server/etc/dovecot/dovecot.conf" /etc/dovecot/dovecot.conf
    fi
fi
if [ -f etc/dovecot/passwd ] || [ -d etc/dovecot/passwd ] ; then
    read -p "copy /etc/dovecot/passwd ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 600 "server/etc/dovecot/passwd" /etc/dovecot/passwd"
        install -D -d -o root -g root -m 600 "server/etc/dovecot/passwd" /etc/dovecot/passwd
    fi
fi
if [ -f var/mails/globalsieve.sieve ] || [ -d var/mails/globalsieve.sieve ] ; then
    read -p "copy /var/mails/globalsieve.sieve ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o vmail -g vmail -m 644 "server/var/mails/globalsieve.sieve" /var/mails/globalsieve.sieve"
        install -D -d -o vmail -g vmail -m 644 "server/var/mails/globalsieve.sieve" /var/mails/globalsieve.sieve
    fi
fi
if [ -f var/mails/dot_fetchmailrc ] || [ -d var/mails/dot_fetchmailrc ] ; then
    read -p "copy /var/mails/.fetchmailrc ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o vmail -g vmail -m 700 "server/var/mails/dot_fetchmailrc" /var/mails/.fetchmailrc"
        install -D -d -o vmail -g vmail -m 700 "server/var/mails/dot_fetchmailrc" /var/mails/.fetchmailrc
    fi
fi
if [ -f etc/ssl/certs/dovecot.pem ] || [ -d etc/ssl/certs/dovecot.pem ] ; then
    read -p "copy /etc/ssl/certs/dovecot.pem ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/ssl/certs/dovecot.pem" /etc/ssl/certs/dovecot.pem"
        install -D -d -o root -g root -m 644 "server/etc/ssl/certs/dovecot.pem" /etc/ssl/certs/dovecot.pem
    fi
fi
if [ -f etc/ssl/private/dovecot.pem ] || [ -d etc/ssl/private/dovecot.pem ] ; then
    read -p "copy /etc/ssl/private/dovecot.pem ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 600 "server/etc/ssl/private/dovecot.pem" /etc/ssl/private/dovecot.pem"
        install -D -d -o root -g root -m 600 "server/etc/ssl/private/dovecot.pem" /etc/ssl/private/dovecot.pem
    fi
fi
if [ -f etc/samba/smb.conf ] || [ -d etc/samba/smb.conf ] ; then
    read -p "copy /etc/samba/smb.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/samba/smb.conf" /etc/samba/smb.conf"
        install -D -d -o root -g root -m 644 "server/etc/samba/smb.conf" /etc/samba/smb.conf
    fi
fi
if [ -f etc/asound.conf ] || [ -d etc/asound.conf ] ; then
    read -p "copy /etc/asound.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/asound.conf" /etc/asound.conf"
        install -D -d -o root -g root -m 644 "server/etc/asound.conf" /etc/asound.conf
    fi
fi
if [ -f etc/default/tlp ] || [ -d etc/default/tlp ] ; then
    read -p "copy /etc/default/tlp ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/default/tlp" /etc/default/tlp"
        install -D -d -o root -g root -m 644 "server/etc/default/tlp" /etc/default/tlp
    fi
fi
if [ -f etc/passwd ] || [ -d etc/passwd ] ; then
    read -p "copy /etc/passwd ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/passwd" /etc/passwd"
        install -D -d -o root -g root -m 644 "server/etc/passwd" /etc/passwd
    fi
fi
if [ -f etc/netctl/ethernet-static ] || [ -d etc/netctl/ethernet-static ] ; then
    read -p "copy /etc/netctl/ethernet-static ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/netctl/ethernet-static" /etc/netctl/ethernet-static"
        install -D -d -o root -g root -m 644 "server/etc/netctl/ethernet-static" /etc/netctl/ethernet-static
    fi
fi
if [ -f etc/locale.conf ] || [ -d etc/locale.conf ] ; then
    read -p "copy /etc/locale.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/locale.conf" /etc/locale.conf"
        install -D -d -o root -g root -m 644 "server/etc/locale.conf" /etc/locale.conf
    fi
fi
if [ -f etc/pacman.conf ] || [ -d etc/pacman.conf ] ; then
    read -p "copy /etc/pacman.conf ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/etc/pacman.conf" /etc/pacman.conf"
        install -D -d -o root -g root -m 644 "server/etc/pacman.conf" /etc/pacman.conf
    fi
fi
if [ -f boot/syslinux/syslinux.cfg ] || [ -d boot/syslinux/syslinux.cfg ] ; then
    read -p "copy /boot/syslinux/syslinux.cfg ? [Y/n]" -n 1 -r
    echo
    if [[ $REPLY =~ ^[Y|y]$ ]] ; then
        echo "install -D -d -o root -g root -m 644 "server/boot/syslinux/syslinux.cfg" /boot/syslinux/syslinux.cfg"
        install -D -d -o root -g root -m 644 "server/boot/syslinux/syslinux.cfg" /boot/syslinux/syslinux.cfg
    fi
fi
